import React from 'react'

export default function Favorites(props) {
    return (
        <section className="favorites">
            <ul>
                FAVORITES
            </ul>
        </section>
    )
}